# Keep the production image-processing stack reproducible. rembg's CPU extra
# installs the ONNX Runtime backend required by new_session().
rembg[cpu]==2.0.75
Pillow==12.1.1
redis==5.0.1
minio==7.2.0
python-dotenv==1.0.0
requests==2.31.0
numpy==2.3.5
opencv-python-headless==4.13.0.92
